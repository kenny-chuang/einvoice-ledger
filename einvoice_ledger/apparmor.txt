#include <tunables/global>

profile einvoice_ledger flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  network,
  capability chown,
  capability dac_override,
  capability setgid,
  capability setuid,

  /app/** r,
  /run.sh ix,
  /usr/** rix,
  /bin/** rix,
  /lib/** mr,
  /lib64/** mr,
  /etc/** r,
  /proc/** r,
  /sys/devices/system/cpu/** r,
  /dev/null rw,
  /dev/urandom r,
  /dev/shm/** rwk,
  /tmp/** rwk,
  /data/** rwk,
}
